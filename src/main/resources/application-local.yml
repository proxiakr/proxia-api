spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/proxia
    username: proxia
    password: proxia
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret-key: local-jwt-secret-key-change-in-production

encryption:
  secret-key: local-encryption-secret-key-change-in-production